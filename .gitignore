# Python
__pycache__/
*.py[cod]
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Environment and secrets
.env
!.env.example
*.key
*.pem
wallets/

# Generated indexes, embeddings, and evidence
data/embeddings/*
data/faiss/*
data/evidence/*
data/web_search_cache/*
build/*
# The compiled ABI is required at runtime to talk to the deployed registry,
# so it ships with the repo. It contains no secrets.
!build/EvidenceRegistry.json

# Keep directory markers
!.gitkeep

# IDE and OS files
.DS_Store
.idea/
.vscode/
*.log
*.ses

# Frontend build and dependency output
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local
frontend/.env.*.local
frontend/tsconfig.tsbuildinfo
