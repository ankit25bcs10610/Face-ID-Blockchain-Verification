# Optional FastAPI base URL. Leave empty to display the disconnected state.
# Set this to the URL of the separately running FastAPI service.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_PIPELINE_ENDPOINT=/pipeline/run
NEXT_PUBLIC_VERIFY_ENDPOINT=/verify
NEXT_PUBLIC_HEALTH_ENDPOINT=/health
